# Setup Tasks

- [ ] Create a `src/utils/hello.ts` file that exports a `greet(name: string): string` function
- [ ] Add a unit test for the `greet` function in `src/utils/hello.test.ts`
- [x] Initialize the project (already done)
